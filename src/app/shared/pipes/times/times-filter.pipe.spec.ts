import { TimesFilterPipe } from './times-filter.pipe';

describe('TimesFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new TimesFilterPipe();
    expect(pipe).toBeTruthy();
  });
});
