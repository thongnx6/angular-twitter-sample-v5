import { DisplayMaxLengthPipe } from './display-max-length.pipe';

describe('TimesDisplayMaxLengthPipeFilterPipe', () => {
  it('create an instance', () => {
    const pipe = new DisplayMaxLengthPipe();
    expect(pipe).toBeTruthy();
  });
});
