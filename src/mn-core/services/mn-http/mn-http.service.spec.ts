import { TestBed, inject } from '@angular/core/testing';
import { MnHttpService } from './mn-http.service';

describe('MnHttpService', () => {
  beforeEach(() => {
    TestBed.configureTestingModule({
      providers: [MnHttpService]
    });
  });
});
